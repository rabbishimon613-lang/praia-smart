# praia smart — runtime deps
#
# All workers in workers/ currently rely on Python stdlib + system tools:
#   - curl                  (HTTP)
#   - pdftotext (poppler)   (used by workers/balneabilidade.py)
#
# No Python packages required yet. If a future scraper needs pdfplumber /
# requests / beautifulsoup4, add them here.
