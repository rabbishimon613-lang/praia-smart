# praia smart — runtime deps
#
# All workers in workers/ currently rely on Python stdlib + system tools:
#   - curl                  (HTTP)
#   - pdftotext (poppler)   (used by workers/balneabilidade.py)
#
# Python packages:
openpyxl>=3.1   # workers/balneabilidade.py — INEA historical XLSX parser
#
# If a future scraper needs pdfplumber / requests / beautifulsoup4, add them
# here.
